// Supabase client removed - System now uses server database engine
export const isSupabaseConfigured = (): boolean => false;
export const configureSupabase = (_url: string, _key: string) => {};
export const supabase = null;
